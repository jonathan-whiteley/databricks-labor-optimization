# Vite env vars consumed at build time. The host needs to match the target
# Databricks workspace; the space_id matches var.genie_space_id in
# databricks.yml. When the panel renders this URL in an iframe, it inherits
# the user's SSO session from the parent Databricks App auth.
VITE_GENIE_SPACE_URL=https://fe-vm-jdub-vm-serverless.cloud.databricks.com/genie/rooms/01f14e48463113eda51eff62f704947a
